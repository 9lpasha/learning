var a, b, s1, s2:integer;
begin
  write('Введите 2 числа через пробел: ');
  readln(s1, s2);
  a := s1;
  b := s2;
  while(a<>b) do
  begin
    if(a > b) then
      a := a - b
    else
      b := b - a;
  end;
  writeln('НОД(', s1, ', ', s2, ') = ', a); 
end.